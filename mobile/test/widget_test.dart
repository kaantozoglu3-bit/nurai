import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — Firebase ve auth entegrasyonu tamamlandıktan sonra güncellenecek
    expect(true, isTrue);
  });
}
